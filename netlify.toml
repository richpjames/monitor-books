[build]
  command = "yarn buildProd"
  publish = "build/"
  functions = "functions/"

[context.production]
  command = "yarn buildProd"
  environment = { REACT_APP_RUN_ENV = "production"}

[context.branch-deploy]
  command = "yarn buildProd"
  environment = { REACT_APP_RUN_ENV = "production"}
